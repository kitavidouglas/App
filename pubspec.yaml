name: my_first_flutter_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  cupertino_icons: ^1.0.6
  # Add or update other dependencies here if needed

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Add or update other dev dependencies here if needed

flutter:
  uses-material-design: true

  assets:
    - assets/
